-for site in icon_sites
  %a.flag(href="{{ site.host }}")
    %img(src="{{ STATIC_URL }}img/site_flags/{{ site.flag }}" width="70px")

:css
  body {
      background-color: #000;
      padding: 40px;
  }