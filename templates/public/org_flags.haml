.welcome-flags
  - for site in linked_orgs
    - if forloop.counter0 == break_pos
      .{style:"clear:both;"}
    %a.flag{ href:"{{ site.host }}" }
      - if site.is_static
        %img{ src:"{{ STATIC_URL }}img/flags/{{ site.flag }}" }
      - else
        %img{ src:"{{ site.flag }}" }
