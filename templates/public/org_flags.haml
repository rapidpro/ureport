.welcome-flags
  - for site in linked_orgs
    %a.flag{ href:"{{ site.host }}" }
      - if site.is_static
        %img{ src:"{{ STATIC_URL }}img/flags/{{ site.flag }}" }
      - else
        %img{ src:"{{ site.flag }}" }
